# feature the bednet shock function

feature the bednet shock function

## Usage

``` r
# S3 method for class 'bednet_shock'
update_function_X(X, xds_obj, feature = "bednet_shock", options = list())
```

## Arguments

- X:

  new parameter values

- xds_obj:

  a **`ramp.xds`** xds_obj object

- feature:

  a string to dispatch

- options:

  a list to configure feature-specific indexing

## Value

sum of squared differences
