# Check the seasonal setup

Check the seasonal setup

## Usage

``` r
# S3 method for class 'Lambda'
check_season_par(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
