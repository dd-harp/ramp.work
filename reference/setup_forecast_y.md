# Forecast a Baseline

Forecast a Baseline

## Usage

``` r
setup_forecast_y(y, xds_obj)
```

## Arguments

- y:

  the interpolation points

- xds_obj:

  a **`ramp.xds`** xds_obj object

## Value

a **`ramp.xds`** xds_obj object
