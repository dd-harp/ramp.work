# Mask data

Mask a subset of data so it is not used for fitting

## Usage

``` r
mask_data(xds_obj, mask_ix)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

- mask_ix:

  the indices of the observations to mask

## Value

an **`xds_obj`**
