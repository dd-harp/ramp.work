# feature the irs contact function

feature the irs contact function

## Usage

``` r
# S3 method for class 'irs_contact'
update_function_X(X, xds_obj, feature = "irs_contact", options = list())
```

## Arguments

- X:

  new parameter values

- xds_obj:

  a **`ramp.xds`** xds_obj object

- feature:

  a string to dispatch

- options:

  a list to configure feature-specific indexing

## Value

sum of squared differences
