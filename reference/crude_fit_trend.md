# Initialize the trend

Before fitting, pre-calibrate the forcing function so that it's "close."

## Usage

``` r
crude_fit_trend(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
