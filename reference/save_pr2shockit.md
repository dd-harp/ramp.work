# Save the history fit_obj

After fit_obj a model to data_obj, save the history. To restore that
history, use \[\]

For set of paired a time series \\X,\\ compute the phase of a seasonal
pattern for the EIR

## Usage

``` r
save_pr2shockit(xds_obj)
```

## Arguments

- xds_obj:

  an `xds` xds_obj

## Value

an `xds` object

## Note

This utility relies on `xds_cohort`
