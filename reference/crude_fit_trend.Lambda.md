# Initialize the trend

For a model forced by emergence, pre-calibrate the forcing function so
that it's close.

## Usage

``` r
# S3 method for class 'Lambda'
crude_fit_trend(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
