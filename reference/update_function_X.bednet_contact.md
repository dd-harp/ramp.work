# feature the bed net coverage function

feature the bed net coverage function

## Usage

``` r
# S3 method for class 'bednet_contact'
update_function_X(X, xds_obj, feature = "bednet_contact", options = list())
```

## Arguments

- X:

  new parameter values

- xds_obj:

  a **`ramp.xds`** xds_obj object

- feature:

  a string to dispatch

- options:

  a list to configure feature-specific indexing

## Value

a **`ramp.xds`** model object
