# Compute

Compute

## Usage

``` r
xds_scaling_Lambda(Lambda, xds_obj, scaling)
```

## Arguments

- Lambda:

  emergence rate

- xds_obj:

  a **`ramp.xds`** xds_obj object

- scaling:

  a list with stored values

## Value

a pair of values
