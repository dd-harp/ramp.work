# feature the bednet shock_size function

feature the bednet shock_size function

## Usage

``` r
# S3 method for class 'bednet_shock_size'
update_function_X(X, xds_obj, feature = "bednet_shock_size", options = list())
```

## Arguments

- X:

  new parameter values

- xds_obj:

  a **`ramp.xds`** xds_obj object

- feature:

  a string to dispatch

- options:

  a list to configure feature-specific indexing

## Value

sum of squared differences
