# Compute the observed phase

For a *Pf*PR time series, \\x\\, fit a spline function

## Usage

``` r
approx_phase(pfpr, jdate)
```

## Arguments

- pfpr:

  a *Pf*PR time series

- jdate:

  the corresponding dates

## Value

a list with the mean peak and the values
