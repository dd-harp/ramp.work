# Change Spline \\t,y\\ Values

Change Spline \\t,y\\ Values

## Usage

``` r
add_fit_spline_ty(new_y, new_t, xds_obj)
```

## Arguments

- new_y:

  the new \\y\\ interpolation point value(s)

- new_t:

  the new \\t\\ interpolation point value(s)

- xds_obj:

  a **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
