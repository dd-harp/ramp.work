# Check the trend setup

Check the trend setup

## Usage

``` r
# S3 method for class 'Lambda'
compile_fit_trend(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
