# Compute the Index of Dispersal for a function F(t) over one year.

Compute the Index of Dispersal for a function F(t) over one year.

## Usage

``` r
compute_IoSD_F(F)
```

## Arguments

- F:

  a function describing a seasonality function (possibly not normalized)
