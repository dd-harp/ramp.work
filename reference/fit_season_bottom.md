# Fit a seasonal pattern

For a *Pf*PR time series, \\x\\, fit a spline function

## Usage

``` r
fit_season_bottom(xds_obj)
```

## Arguments

- xds_obj:

  an `xds` xds_obj

## Value

a list with the mean peak and the values
