# Convert pr to lambda

Use the scaling table to convert a set of pr values into lambda values

## Usage

``` r
xds_pr2Lambda(pr, xds_obj, extend = FALSE)
```

## Arguments

- pr:

  a [vector](https://rdrr.io/r/base/vector.html)

- xds_obj:

  a [list](https://rdrr.io/r/base/list.html)

- extend:

  a [logical](https://rdrr.io/r/base/logical.html) option to determine
  whether to extend outside the range
