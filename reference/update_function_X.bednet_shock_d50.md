# feature the bednet d50 function

feature the bednet d50 function

## Usage

``` r
# S3 method for class 'bednet_shock_d50'
update_function_X(X, xds_obj, feature = "bednet_shock_d50", options = list())
```

## Arguments

- X:

  new parameter values

- xds_obj:

  a **`ramp.xds`** xds_obj object

- feature:

  a string to dispatch

- options:

  a list to configure feature-specific indexing

## Value

sum of squared differences
